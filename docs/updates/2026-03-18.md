# Update Log - 2026-03-18

## Context

Requested restart from zero:

1. Pull latest git with remote-first conflict policy
2. Re-analyze the repository
3. Create a continuously maintainable docs area
4. Focus on `q3_mouse`, `q3_hang_cup`, UNet single-frame, Transformer single-frame

## Actions Completed

1. Re-fetched and re-synced to `origin/main`.
2. Verified local and remote are aligned at `ba8ef9f`.
3. Confirmed `q3_hang_cup.yaml` exists under `diffusion_policy/config/task/`.
4. Created docs area:
   - `docs/README.md`
   - `docs/project-analysis.md`
   - `docs/task-workspace-playbook.md`
   - `docs/updates/2026-03-18.md` (this file)

## Key Findings

1. Current Makefile defaults already match intended daily usage:
   - `TASK := q3_hang_cup`
   - `WKSPACE := train_diffusion_unet_timm_single_frame_workspace`
2. Both `q3_mouse` and `q3_hang_cup` rely on cloud dataset wrapper with local zarr cache support.
3. `HOMMI` is a custom Transformer setup and differs from baseline Transformer single-frame aggregation logic.

## Suggested Next Items

1. Add a training result template (ckpt, val loss, deployment notes).
2. Add recommended hyperparameter table for `q3_mouse` and `q3_hang_cup`.
3. Decide whether to remove/replace temporary rollout runner hard-coding in UNet workspace.

## Branch Kickoff: Progress-Aware Sampling

Branch: `codex/progress-aware-sampling`

Documentation milestone completed:

1. Added `docs/experiments/progress-aware-sampling/design.md`
2. Added `docs/experiments/progress-aware-sampling/progress-label-spec.md`
3. Added `docs/experiments/progress-aware-sampling/ablation-plan.md`
4. Added `docs/experiments/progress-aware-sampling/doc-plan.md`

This branch is now ready for implementation discussion and code-phase breakdown.

## Follow-up Alignment: Label Extraction References

Incorporated user-provided references into branch docs:

1. `post_process_scripts/post_process_data_vr_mouse.py`
   - keep aruco path out of the progress-label critical path
   - keep SAM hand masking as optional and decoupled
2. `scripts/analyze_gripper_trend.py`
   - use trend segmentation (`flat/up/down`) and short-window merge ideas
3. `sign_accuracy_pick_place.py`
   - use closing-window detection logic near gripper-width minimum

The spec now uses a hybrid label extraction approach and adds a required label-quality dry-run stage before sampler training ablations.

## Priority Shift: General Sampler First

Based on follow-up alignment:

1. Direction labels from absolute action coordinates are not reliable for general use.
2. Highest priority is now embedding-based cross-view sampling:
   - wrist embedding close
   - head embedding far
3. Progress/direction labels are downgraded to optional enhancement path.

Documentation updated accordingly:

1. Added `docs/experiments/progress-aware-sampling/embedding-sampler-spec.md`
2. Reordered design and ablation plans around embedding-first experiments
3. Marked progress-label spec as optional add-on

## Priority Refinement: Future-Action Difference is Required

Updated decision:

1. Candidate ranking is not `head-distance` only.
2. Required joint score now combines:
   - head embedding distance
   - future-action distance
3. Default starting weights are documented as `alpha=0.5`, `beta=0.5` and will be tuned in E2.

## Additional Constraint: Encoder Must Match Training Config

Locked requirement:

1. Sampling-analysis encoder must match training encoder family/config.
2. Cache artifacts must include encoder signature metadata and enforce rebuild on mismatch.

## Implementation Progress: Structured Sampling V1 Skeleton

Implemented and tested:

1. `diffusion_policy/common/structured_sampling_meta.py`
   - cache metadata/signature build, save/load, compatibility check
2. `diffusion_policy/common/structured_sampling_index.py`
   - future-action features, exact chunked wrist-KNN, joint-score ranking
3. `diffusion_policy/common/structured_batch_sampler.py`
   - coverage-first structured batch construction with optional candidate lists
4. `diffusion_policy/common/structured_dataloader.py`
   - unified train loader builder with optional structured sampler
5. Workspace integration
   - `train_diffusion_unet_timm_workspace.py`
   - `train_diffusion_transformer_timm_workspace.py`
6. Diagnostics
   - `scripts/analyze_wrist_knn_clusters.py`

Validation:

1. Unit tests passed:
   - `test_structured_sampling_meta.py`
   - `test_structured_sampling_index.py`
   - `test_structured_batch_sampler.py`
   - `test_structured_dataloader.py`
2. Syntax check passed for structured dataloader + both timm workspaces.

## Clarification: Batch-Size-Aware Cap

For any class/group cap in structured selection, cap should scale with structured batch budget:

1. `B_struct = round(B * p_structured)`
2. `max_per_class = max(1, ceil(rho * B_struct))`
3. default `rho = 0.4`

This avoids fixed-cap underflow when `B` increases and keeps enough room to pick high `d_head + d_action` samples.

## Real Dataset Smoke Tests (q3_mouse, UNet single-frame)

Local dataset path used:

- `C:/Users/yibo/Downloads/umi_base/.cache/q3_mouse_dh_train/replay_buffer.zarr`

Run budget policy:

1. short smoke only (`num_epochs=1`, `max_train_steps=1`, `max_val_steps=1`)
2. no long training in this phase

### Baseline smoke (structured OFF)

Output:

- `data/outputs/2026.03.18/17.43.01_train_diffusion_unet_timm_q3_mouse_rawresize_sharevit`

Key log values (epoch 0):

1. `train_loss=1.1755`
2. `val_loss=1.2106`
3. `train_action_mse_error=0.6372`
4. `val_action_mse_error=0.6506`

### Structured smoke (structured ON)

Command override highlights:

1. `+dataloader.structured_sampling.enabled=true`
2. `+dataloader.structured_sampling.candidate_indices_path=.../candidate_indices_ring_top192.npy`
3. `+dataloader.structured_sampling.structured_ratio=0.5`

Output:

- `data/outputs/2026.03.18/18.10.32_train_diffusion_unet_timm_q3_mouse_rawresize_sharevit`

Key log values (epoch 0):

1. `train_loss=1.1872`
2. `val_loss=1.2130`
3. `train_action_mse_error=0.6406`
4. `val_action_mse_error=0.6453`

## Proof That New Batch Sampler Is Active

Direct dataloader introspection on real `q3_mouse` config:

1. structured OFF:
   - `sampler_obj = None`
   - `batch_sampler_obj = BatchSampler`
2. structured ON:
   - `sampler_obj = StructuredCoverageBatchSampler`
   - `batch_sampler_obj = StructuredCoverageBatchSampler`
   - `is_structured_sampler = True`

Conclusion:

1. new batch sampling path is online and switchable by config
2. real-data smoke tests complete with no runtime failure

## Robustness Update: Missing Candidate File Auto-Prepare

Observed issue in multi-GPU server run:

1. structured candidate file path missing caused `FileNotFoundError`
2. one rank failed and triggered NCCL group abort

Fixes added:

1. new script:
   - `scripts/generate_structured_ring_candidates.py`
   - generates fallback ring candidates directly from local replay zarr
2. Makefile target:
   - `prepare_structured_candidates`
   - auto-generates candidate file if missing
3. `train_acc8_amp_structured` now depends on `prepare_structured_candidates`
4. structured dataloader now raises actionable error message with generation hint

## Dataset Cleanup: Remove Legacy Wrist Mask Trick

Decision:

1. remove random wrist image masking in `RealPickAndPlaceImageHeadDataset.__getitem__`
2. avoid confounding the effect of new structured batch sampling

Code changes:

1. `diffusion_policy/dataset/real_pick_and_place_image_head_dataset.py`
   - deleted wrist random-occlusion logic in RGB loading path
   - cleaned related dead code / stale imports

Expected behavior:

1. wrist image distribution remains natural (no synthetic black-frame wrist perturbation)
2. head-dependency gains should be attributable to sampling method, not image masking trick
